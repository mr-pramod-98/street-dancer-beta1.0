<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/parent_layout">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar_user_uploads"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/toolbar_background"
            app:titleTextColor="#FFF"
            style="@style/ToolbarTheme"
            app:title="My posts" />

        <com.example.street_dancer_beta10.SharedComponents.RecyclerView.VideoPlayerRecyclerView
            android:id="@+id/profile_user_upload_recycler_view"
            android:layout_below="@+id/toolbar_user_uploads"
            android:layout_centerVertical="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

        </com.example.street_dancer_beta10.SharedComponents.RecyclerView.VideoPlayerRecyclerView>

    </RelativeLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>